# Dependency declarations live in pyproject.toml (PEP 621 extras).
#
# Full dev environment (server + pipeline + ML + test tooling):
#     pip install -e .[server,pipeline,ml,dev]
#
# Production pipeline venv (module set the nightly preflight checks):
#     pip install -e .[pipeline,ml]
#
# Flask/uwsgi web server only:
#     pip install -e .[server]
#
# Installing this file gives the full dev environment:
-e .[server,pipeline,ml,dev]
